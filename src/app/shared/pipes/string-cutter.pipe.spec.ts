import { StringCutterPipe } from './string-cutter.pipe';

describe('StringCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
